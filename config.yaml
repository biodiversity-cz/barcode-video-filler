camera_id: /dev/video10
regex: "^PRC[\\s\\--_](?P<numericPart>\\d+)$"  # RegEx for barcode parsing
digits: 7
filename_template: "PRC_{numeric_part}_{timestamp}_robot" # must include {numeric_part} and {timestamp} string
sound:
  success: "./sound/ok.wav"
  lost: "./sound/lost.wav"

